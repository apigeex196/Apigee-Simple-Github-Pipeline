You have access to this repository. Update only docs/api-producer-core.md.

Hard requirement: After your edits, the documentation must match the GitHub workflow path filters and behavior exactly. Do not claim anything is automated unless the workflows actually do it.

Do not edit workflows, schemas, templates, or any other file.

Known workflow truth (use this as source of truth)

Proxy validation/deploy triggers on:

SYSGEN*_*/dev/proxies/**/*.yaml

SYSGEN*_*/test/proxies/**/*.yaml

SYSGEN*_*/prod/proxies/**/*.yaml

Product validation/deploy triggers on:

SYSGEN*_*/dev/products/*.yaml

SYSGEN*_*/test/products/*.yaml

SYSGEN*_*/prod/products/*.yaml

global/dev/products/*.yaml

global/test/products/*.yaml

global/prod/products/*.yaml

Workflows validate schema only when kind matches expected (ApiProxy / ApiProduct).

Filenames under these paths can be any *.yaml (not necessarily proxy.yaml), but proxy.yaml may be recommended as a convention.

PATCH LIST (apply all)
PATCH 1 — Replace MAL structure everywhere with SYSGEN folder structure

In the entire doc, replace wording and paths like:

“MAL folder”

mal-<SYSGEN_CODE>/...

orgs/<org>/envs/<env>/proxies/<project>/...

mal-<SYSGEN>/orgs/<org>/products/...

With the canonical structure that matches workflows:

Canonical proxy YAML location

SYSGEN<9digits>_<TEAM-or-APP>/(dev|test|prod)/proxies/(public|private)/<env-name>/<any>.yaml

Include one explicit example line:
SYSGEN788836350_APIGEE-CC/test/proxies/private/test1/proxy.yaml

Canonical product YAML location

SYSGEN<9digits>_<TEAM-or-APP>/(dev|test|prod)/products/<any>.yaml

Also mention global product path (for shared/central products):
global/(dev|test|prod)/products/<any>.yaml

Do not keep any mal-... or orgs/.../envs/... references anywhere.

PATCH 2 — Fix Section 1: Repo structure bullet + Repo Layout example

Update Section 1 “Repo structure” bullet to describe the SYSGEN path pattern above.
Update “Repo Layout (MAL)” heading to “Repo Layout (SYSGEN)” and update all bullets accordingly.

Replace the folder tree example with:

SYSGEN788836350_APIGEE-CC/
test/
proxies/
private/
test1/
proxy.yaml
your-openapi.yaml
products/
SYSGEN788836350-my-product.yaml
dev/
proxies/
private/
dev1/
proxy.yaml
global/
test/
products/
shared-product.yaml

Keep the document consistent.

PATCH 3 — Update Quickstart (Windows) to create files in the SYSGEN structure

Rewrite the Quickstart variable section to:

$sysgen = "SYSGEN788836350"
$team = "APIGEE-CC"
$stage = "test" # dev | test | prod
$visibility = "private" # public | private
$envName = "test1"
$project = "$sysgen-E2E-TEST-BASIC"
$root = "$sysgen`_$team/$stage/proxies/$visibility/$envName"

Then create proxy.yaml under $root (same schema-valid YAML content).

Also update the yq eval command paths to point to $root/proxy.yaml.

PATCH 4 — OAS placement examples must match workflow paths

Wherever you show OAS placement, update examples to:

SYSGEN788836350_APIGEE-CC/test/proxies/private/test1/
proxy.yaml
your-openapi.yaml

Keep the wording: “same directory as your proxy.yaml”.

Update any remaining MAL-based OAS path string.

PATCH 5 — Products: remove false “generated by workflows” claims

In the “Products” section, remove this claim:

“Products are generated by workflows based on your proxy configuration.”

Replace with accurate wording:

“Products are deployed from Product YAML files committed under the products paths validated by workflows.”

In Section 3 “Create / Customize API Products”, update “Location in MAL structure” to the SYSGEN products path.

Add a short subsection called “Product Source of Truth”:

If a Product YAML exists in SYSGEN*_*/(dev|test|prod)/products/, workflows validate and deploy it.

Global products may be managed under global/(dev|test|prod)/products/.

If no Product YAML is provided, product behavior depends on platform defaults (do not claim auto-generation unless workflow implements it).

PATCH 6 — Replace placeholders in Requesting Access & Provisioning without inventing internal details

Replace <GitHub Group>, <Submit Jira ticket to Platform Team>, and <Platform Contact> with safe references:

Point to docs/repository-configuration.md as authoritative for access and escalation.
Do not invent emails, slack channels, group names, or ticket queues.

Example wording:
“Follow access and ownership guidance in docs/repository-configuration.md.”

PATCH 7 — Fix Promotion Guidance example paths

Replace the “Example paths” block that currently shows MAL/org/env and base.yaml.
Update it to show SYSGEN structure and use proxy.yaml.

Example:

SYSGEN788836350_APIGEE-CC/
dev/proxies/private/dev1/proxy.yaml
test/proxies/private/test1/proxy.yaml
prod/proxies/private/prod1/proxy.yaml

Also add one note:

“Workflows trigger on any *.yaml under these paths; proxy.yaml is the recommended convention.”

OUTPUT REQUIREMENTS

Show a unified diff for docs/api-producer-core.md only.

Ensure there are ZERO remaining strings matching:

mal-

orgs/

envs/

base.yaml

“Products are generated by workflows”

placeholder tokens like <GitHub Group>

Keep existing links to schema files and failure-modes docs unless path changes are necessary.

Keep tone professional and concise.

Begin.
